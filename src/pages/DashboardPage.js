import React from 'react';
import Button from '../components/common/Button';

const DashboardPage = () => {
    return (
        <div>
            <Button>대시보드</Button>
        </div>
    );   
};

export default DashboardPage;